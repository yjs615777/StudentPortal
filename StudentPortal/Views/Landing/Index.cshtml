@{
}
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="utf-8" />
    <title>Student Portal</title>
    <meta name="viewport" content="width=1440, initial-scale=1" />
    <!-- Inter 폰트 -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@500;700&display=swap" rel="stylesheet">
    <style>
        /* 화면 프레임: W1440 × H1071, 중앙 정렬 */
        html, body {
            height: 100%;
            margin: 0;
            background: #FFFFFF;
        }

        .stage {
            width: 1440px;
            height: 1071px;
            margin: 0 auto; /* 좌우 중앙 */
            display: flex;
            align-items: center; /* 세로 중앙 */
            justify-content: center; /* 가로 중앙 */
        }

        .stack {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 43px; /* 타이틀과 버튼 사이 간격 H43 */
        }

        .title {
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", sans-serif;
            font-size: 64px; /* 크기 64 */
            font-weight: 700; /* Bold */
            line-height: 1.1;
            color: #000000; /* 검정 */
            margin: 0;
        }

        .btn-main {
            width: 159px; /* 버튼 W159 */
            height: 76px; /* 버튼 H76 */
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", sans-serif;
            font-size: 24px; /* 글자 크기 24 */
            font-weight: 500; /* Medium */
            color: #FFFFFF; /* 검정 텍스트 */
            background: #1491FF; /* 버튼 하늘색 */
            border: 1px solid #1491FF; /* 테두리(옵션: 피그마에 없으면 제거) */
            border-radius: 8px; /* 모서리(원하면 0으로) */
            text-decoration: none; /* 링크 밑줄 제거 */
            cursor: pointer;
        }

            .btn-main:active {
                transform: translateY(1px);
            }
        /* 필요시 포커스/호버 스타일 추가 가능 */
    </style>
</head>
<body>
    <div class="stage">
        <div class="stack">
            <h1 class="title">Student Portal</h1>

            <!-- 학생 조회 버튼: StudentController.Index로 이동 -->
            <a asp-controller="Student" asp-action="Index" class="btn-main">학생 조회</a>
        </div>
    </div>
</body>
</html>